-- a script that lists all records of the table second_table
-- in the current database in the MySQL server
SELECT score, name FROM second_table WHERE name <> "" ORDER BY score DESC-- a script that lists all records of the table second_table
-- in the current database in the MySQL server
SELECT score, name FROM second_table WHERE name <> "" ORDER BY score DESC
